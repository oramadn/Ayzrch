[Unit]
# Orbit's niri session pulls this unit through niri-session.target. PartOf is
# additive: the unit's own PartOf=hyprland-session.target still applies, so this
# only adds the niri session to the set of targets that stop it.
PartOf=niri-session.target
