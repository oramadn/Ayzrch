#!/bin/sh
# Hyprland session path to the locker used by orbit-session-transition. The
# implementation is compositor-neutral and lives in bin/orbit-noctalia-lock.

set -eu

exec "$HOME/.local/bin/orbit-noctalia-lock"
