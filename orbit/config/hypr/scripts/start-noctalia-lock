#!/bin/sh
# Hyprland session entry point for the lock. The choreography is
# compositor-neutral and lives in bin/orbit-lock, which the niri session binds
# directly; this keeps the Hyprland path that hyprland.lua and hypridle expect.

set -eu

exec "$HOME/.local/bin/orbit-lock"
