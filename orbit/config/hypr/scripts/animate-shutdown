#!/bin/sh
# Hyprland session entry point for end-of-session actions. The confirmation and
# hand-off are compositor-neutral and live in bin/orbit-power.

set -eu

exec "$HOME/.local/bin/orbit-power" "$@"
