-- Compositor-owned workspace overview used by workspace-oriented Alt-Tab.

hl.config({
    plugin = {
        scrolloverview = {
            gesture_distance = 300,
            scale = 0.8,
            workspace_gap = 0,
            layout = "vertical",
            wallpaper = 0,
            blur = false,
            shadow = { enabled = false, range = 24 },
        },
    },
})
